{
  "name": "docker-ubuntu-nvm",
  "version": "0.1.0",
  "description": "An ubuntu 18.04 image with nvm installed",
  "scripts": {
    "build": "docker build -t keymux/docker-ubuntu-nvm .",
    "publish": "scripts/publish.sh",
    "start": "docker run --rm -ti keymux/docker-ubuntu-nvm /bin/bash"
  },
  "license": "MIT"
}
